extends base.pug


block content 
    video(src="/"+video.fileUrl, controls,  width="700", height="700")
    div
        p=video.description
        small=video.createdAt
    br
    a(href=`${video.id}/edit`) Edit Video &rarr;
    br
    a(href=`${video.id}/delete`) Delete Video &rarr;